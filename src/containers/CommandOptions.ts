enum CommandOptions {
  debug = 'debug',
  name = 'name',
  user = 'user',
  value = 'value',
  channel = 'channel',
  whitelist = 'whitelist',
  min = 'min',
  max = 'max',
  seconds = 'seconds',
}

export default CommandOptions;
